// Number
let age: number = 25;

// String
let name: string = "Aman";

// Boolean
let isOnline: boolean = true;

// Array
let scores: number[] = [10, 20, 30];   // array of type number
console.log(scores[0]);

// Tuple
let user: [string, number] = ["Aman", 23];  //basically u  define the structure of the array before only 

// Enum
enum Role {
    Admin,
    User,
    Guest
}


// Any
let something: any = "can be anything";

// Void
function log(): void {
    console.log("Hi");
}

// Null / Undefined
let x: null = null;